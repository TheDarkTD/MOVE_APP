<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">


    <!-- ======== Rede e Internet ======== -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- ======== Wi-Fi (opcional, se ainda detectar IP) ======== -->
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />

    <!-- ======== Bluetooth ======== -->
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />

    <!-- Android 12+ requer permissões específicas de runtime -->
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADVERTISE" />

    <!-- ======== Localização (necessária para BLE em Android <12) ======== -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />

    <!-- ======== Notificações (Android 13+) ======== -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- ======== Armazenamento (Firebase + Logs locais) ======== -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />

    <!-- ======== Serviço em primeiro plano (caso BLE rode em Service) ======== -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />



    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/logostmove_claro"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round_foreground"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication2"
        tools:targetApi="31"
        android:usesCleartextTraffic="true"
        >

        <service
            android:name=".DataCaptureService"
            android:exported="false" />

        <activity

            android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".Register.RegisterActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.DEFAULT" />
            </intent-filter>
        </activity>
        <activity android:name=".Register.Register9Activity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.DEFAULT" />
            </intent-filter>
        </activity>
        <activity android:name=".Home.HomeActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.DEFAULT" />
            </intent-filter>
        </activity>


        <!-- Hub de pacientes: tela com "Buscar" ou "Cadastrar" -->
        <activity
            android:name=".patient.PatientHubActivity"
            android:exported="false" />

        <!-- Buscar paciente cadastrado -->
        <activity
            android:name=".patient.PatientSearchActivity"
            android:exported="false" />

        <!-- Cadastrar novo paciente -->
        <activity
            android:name=".patient.PatientFormActivity"
            android:exported="false" />

        <!-- Escolha do modo do exame (Estático x Movimento) -->
        <activity
            android:name=".exam.ExamModeActivity"
            android:exported="false"
            android:parentActivityName=".patient.PatientHubActivity">
            <!-- Suporte ao "up button" em versões antigas -->
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".patient.PatientHubActivity" />
        </activity>


    </application>

</manifest>
